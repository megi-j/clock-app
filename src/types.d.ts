export type ClockType = {
  timezone: string;
  day_of_year: number;
  day_of_week: number;
  week_number: number;
  datetime: string;
};

export type QuoteType = {
  content: string;
  author: string;
};
